@model FuncaoViewList

@{
    Layout = "Layout";
    ViewData["Title"] = "Funções";
}
<p></p>
<p></p>
<h2>Funções</h2>

<div class="container">
    <div class="panel panel-default">
        <div class="panel-heading">Pesquisa</div>
        <div class="panel-body">
            <form asp-action="Index">
                <div class="form-group">
                    <label asp-for="CurrentNome" class="control-label"></label>
                    <input asp-for="CurrentNome" class="form-control" />
                </div>
                <div class="form-group">
                    <input type="submit" value="Pesquisar" class="btn btn-default" />
                </div>
            </form>
        </div>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                FuncaoID
            </th>
            <th>Nome</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Funcao) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FuncaoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FuncaoID" class="btn btn-warning">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.FuncaoID" class="btn btn-info">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.FuncaoID" class="btn btn-danger">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-primary">Adcionar Função</a>
</p>