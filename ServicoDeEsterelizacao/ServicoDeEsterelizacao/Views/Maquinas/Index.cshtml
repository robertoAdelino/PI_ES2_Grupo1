@model IEnumerable<ServicoDeEsterelizacao.Models.Maquinas>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Capacidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Capacidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaquinasId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaquinasId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaquinasId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
