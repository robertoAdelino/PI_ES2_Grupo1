@model ServicoDeEsterelizacao.Models.Posto

@{
    Layout = "Layout";
    ViewData["Title"] = "Details";
}

<h2>Detalhes</h2>

<div>
    <h4>Posto</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.PostoId" class="btn btn-warning">Editar</a> |
    <a asp-action="Index" class="btn btn-default">Voltar atrás</a>
</div>
