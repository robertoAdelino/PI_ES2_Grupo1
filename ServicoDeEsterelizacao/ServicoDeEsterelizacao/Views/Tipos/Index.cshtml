@model IEnumerable<ServicoDeEsterelizacao.Models.Tipo>

@{
    Layout = "Layout";
    ViewData["Title"] = "Index";
}
<p></p>
<p></p>
<p></p>
<p></p>
<p></p>
<p></p>
<h2>Tipos de Equipamento</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.TipoID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.TipoID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.TipoID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-default">Create New</a>
</p>